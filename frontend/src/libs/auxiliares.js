export function selectWithRarity (arr){
    const randIndex = Math.floor(Math.random()*Math.random()*arr.length);
    return arr[randIndex];
}

export function fight(team1Stats, stage) {

    let res = Math.random()

    return (res < 1/stage)
}

